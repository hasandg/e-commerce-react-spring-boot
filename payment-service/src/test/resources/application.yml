spring:
  application:
    name: payment-service-test
  cloud:
    config:
      enabled: false
  data:
    mongodb:
      uri: mongodb://localhost:27017/payment-test-db

eureka:
  client:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health,info 